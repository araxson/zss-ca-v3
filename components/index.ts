/**
 * Shared Components
 *
 * This is the root barrel export for all shared components.
 * All components must be imported through this index file.
 *
 * Organization:
 * - layout/ = Layout components (header, footer, dashboard)
 * - error-boundaries/ = Error boundary components
 * - ui/ = shadcn/ui components (NEVER EDIT)
 */

// Layout Components
export * from './layout'

// Error Boundary Components
export * from './error-boundaries'
